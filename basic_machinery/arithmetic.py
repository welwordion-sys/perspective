# ARCHIVED — superseded by builders/arithmetic_spine.py
#
# This file contained the flat-encoding addition rule builders
# (add_init, bit_add, drain, add_finalise, tombstone_gc).
# The flat encoding is no longer used. The spine-encoded pipeline
# lives in builders/arithmetic_spine.py and passes the full 0-7+0-7 battery.
